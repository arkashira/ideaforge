# Idea Generator
A simple Python project that generates software tool ideas based on user interests and skills.

## Usage
Run the `main.py` file to generate ideas.

## Tests
Run the tests using `pytest` command.
