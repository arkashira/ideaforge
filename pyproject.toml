[project]
name = "ideaforge"
version = "0.1.0"
description = "Core Ideation Engine for generating ranked software tool ideas."
authors = [{name = "Axentx"}]
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
